import { AppointmentsClient } from "@/components/admin/AppointmentsClient";

export const metadata = {
  title: "Görüşlər · Alliance Admin",
};

export default function AdminAppointmentsPage() {
  return <AppointmentsClient />;
}
