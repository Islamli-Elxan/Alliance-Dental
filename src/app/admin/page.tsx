import { DashboardClient } from "@/components/admin/DashboardClient";

export const metadata = {
  title: "İdarə paneli · Alliance Admin",
};

export default function AdminDashboardPage() {
  return <DashboardClient />;
}
